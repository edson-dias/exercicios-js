function ProgressaoAritmetica(numero, primeiroTermo, razao){
    return (primeiroTermo + (numero -1)*razao)
}

let progressaoGeometrica = (numero, primeiroTermo, razao) => {
    return (primeiroTermo * Math.pow(razao, (n-1)))
}
